npx create-react-app react_app
でreactアプリを作成

cd でアプリフォルダへ移動

npm start
で実行

eslint
コードを保管ツール

scssの導入
yarn add node-sass
npm install -s node sass
yarn add node-sass

component
・functionl 関数定義
・class クラス定義

classを使用する際は
{ component }を記載し、
クラス作成時に継承する
